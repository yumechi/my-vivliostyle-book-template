[tools]
node = "24.14.1"
pnpm = "10.33.0"

[env]
_.path = ["./node_modules/.bin"]

[tasks.install]
run = "pnpm install"
description = "依存パッケージのインストール"

[tasks.build]
run = "pnpm run build"
description = "PDF のビルド"
depends = ["install"]

[tasks.preview]
run = "pnpm run preview"
description = "プレビューの起動"
depends = ["install"]

[tasks.lint]
run = "pnpm run lint"
description = "textlint の実行"
depends = ["install"]
